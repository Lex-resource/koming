# ============ 模型 SDK ============
openai>=1.35.0

# ============ 向量数据库（真实向量记忆）============
chromadb>=0.5.3

# ============ 浏览器自动化（搜索+抓取）============
playwright>=1.44.0

# ============ Web 服务 ============
fastapi>=0.111.0
uvicorn>=0.30.1
pydantic>=2.7.0

# ============ python-multipart（HTTP 文件上传，语音 ASR 用）============
python-multipart>=0.0.9

# ============ 工具 ============
python-dotenv>=1.0.1
requests>=2.32.0
beautifulsoup4>=4.12.0
